# Configuração para ambiente Docker
spring.application.name=jotanunes

# Configuração do banco de dados PostgreSQL
# Usando o nome do serviço do docker-compose como hostname
spring.datasource.url=jdbc:postgresql://postgres:5432/system
spring.datasource.username=postgres
spring.datasource.password=1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurações de e-mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=jotanunes.corporativo@gmail.com
spring.mail.password=ipassqsnlrpuuit
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuração do RabbitMQ
# Usando o nome do serviço do docker-compose como hostname
spring.rabbitmq.host=rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
spring.rabbitmq.queue=ms.email

# Configurações de segurança
api.security.token.secret=${JWT_SECRET:my-secret-key}

# Configurações do Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}
